name: production-calculator-dev
services:
  backend:
    container_name: backend-dev
    ports:
      - 5141:5000
    environment:
      - ASPNETCORE_ENVIRONMENT=Development

  postgres:
    container_name: postgres-dev
    ports:
      - 5151:5432

  flyway:
    container_name: flyway-dev